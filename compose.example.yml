services:
  notifyrouter:
    build: .
    restart: unless-stopped
    env_file:
      - /path/outside/repository/notifyrouter.env
    volumes:
      - notifyrouter-data:/data
    ports:
      - "127.0.0.1:8080:8080"

volumes:
  notifyrouter-data:

